describe('App', () => {
  beforeEach(() => {
    cy.visit('http://localhost:3000');
  });
  it('shows the landing page', () => {
    
  })
  it('shows the header', () => {
    cy.visit('http://localhost:3000/')
  })
  it('shows landing information', () => {
    
  })
  it('shows a generic pie chart', () => {
    
  })
  it('has a button to get started', () => {
    
  })
})